# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# https://github.com/Parad1st

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -XX:+HeapDumpOnOutOfMemoryError -Dkotlin.daemon.jvm.options\="-Xmx4096m"

# Enable parallel build for faster compilation
org.gradle.parallel=true

# Enable configuration on demand
org.gradle.configureondemand=true

# Enable build caching
org.gradle.caching=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Automatically convert third-party libraries to use AndroidX
# Note: Jetifier is deprecated but still needed for some old libraries
android.enableJetifier=true

# This is deprecated in AGP 8.0 - REMOVE or comment out
# android.useDeprecatedNdk=true

# This is deprecated - REMOVE or comment out
# android.overridePathCheck=true

# Fix for AGP 8.0+ - prevents duplicate resource errors
android.nonTransitiveRClass=true

# Enable namespacing for libraries (required for AGP 8.0+)
android.enableNamespaceInPaths=true

# Disable pre-dexing for faster builds on modern systems
android.dexing.incremental=false
